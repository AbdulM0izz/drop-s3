# drop-s3
This is a React file uploader that enables file uploads directly to an AWS S3 bucket. It uses AWS SDK v3 for secure client-side file uploads and offers real-time upload progress updates. Suitable for adding file upload functionality in React apps with minimal configuration
